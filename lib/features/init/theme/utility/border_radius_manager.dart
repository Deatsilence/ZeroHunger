import 'package:flutter/material.dart';

class ProjectBorderRadiusUtility {
  final BorderRadius normalBorderRadius = BorderRadius.circular(10);
  final BorderRadius buttonBorderRadius = BorderRadius.circular(20);
  final BorderRadius categoryBorderRadius = BorderRadius.circular(60);
}

class ProjectRadiusUtility {
  final Radius scrollBarRadiusCircular = const Radius.circular(20);
  final Radius DraggableScrollableSheetRadiusCircular = const Radius.circular(20);
}
